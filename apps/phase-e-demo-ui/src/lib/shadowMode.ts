/**
 * READ-ONLY SHADOW MODE CONFIGURATION
 * 
 * This is the single source of truth for the assistant's data source mode.
 * When SHADOW_MODE is true, the assistant will attempt to use live, production-shaped
 * data sources in a strictly read-only capacity.
 */

// Read-only shadow mode. No execution permitted.
export const SHADOW_MODE = true;
