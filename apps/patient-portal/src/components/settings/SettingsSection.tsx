
export const SettingsSection = ({ title, children }: any /* eslint-disable-line @typescript-eslint/no-explicit-any -- TODO: fix legacy types */) => (
  <div>
    <h3>{title}</h3>
    {children}
  </div>
);
