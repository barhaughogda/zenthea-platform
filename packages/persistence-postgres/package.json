{
  "name": "@zenthea/persistence-postgres",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PostgreSQL persistence adapters for EHR Core domains. Raw SQL only, no ORMs.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate": "node --import tsx src/migration-runner.ts"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "tsx": "^4.19.2"
  },
  "peerDependencies": {
    "@zenthea/ehr-core": "workspace:*"
  }
}
